import { Layout } from '@/components/Layout';
import CortinerosList from './CortinerosList';

const Cortineros = () => {
  return (
    <Layout>
      <CortinerosList />
    </Layout>
  );
};

export default Cortineros;
